🛍️ NovaShop

NovaShop is a modern and responsive e-commerce frontend application built using React + Vite.
The project demonstrates reusable component architecture, dynamic UI rendering, and clean state management using React Hooks.
